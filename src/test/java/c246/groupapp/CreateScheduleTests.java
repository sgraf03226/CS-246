package c246.groupapp;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CreateScheduleTests {
    /* Tests For The UI of the Create schedule
    step 1: open menu and click the make schedule button. Ensure that the make schedule activity pulls up
    step 2: Create a schedule that is later than todays current date.
    step 3: watch for toast. Make sure the schedule creation was successful.
    step 4: hit the back button to ensure that the schedule item was created

     */
}
